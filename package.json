{
    "scripts": {
        "dev": "astro dev --port 4000",
        "wrangler-dev": "npm run build && wrangler dev --port 4000",
        "build": "astro build",
        "preview": "astro preview"
    },
    "devDependencies": {
        "@types/node": "^25.9.4",
        "astro": "^7.3.4",
        "fast-xml-parser": "^5.8.0",
        "nodemon": "^3.1.14",
        "typescript": "^6.0.3",
        "wrangler": "^4.137.0"
    },
    "dependencies": {
        "fast-xml-builder": "^1.3.0",
        "he": "^1.2.0"
    },
    "engines": {
        "node": ">=22"
    }
}
